<h2>Welcome!</h2>

<% if @current_user.present? %>
  <p> You are logged in as <%= @current_user.name %></p>
  <p> Your your booked flights and book new ones using the links below </p>

  <%= link_to "View Your Flights", user_path(@current_user.id) %>
  <%= link_to "Search For Flights", search_flights_path %>
  <%= link_to "View All Flights", flights_path %>

<% else %>
  <p>To Start Booking Flights, Sign Up or Log In</p>

  <%= link_to "Sign up", new_user_path %>
  <%= link_to "Login", sessions_new_path %>

<% end %>
