<h2>User List</h2>

<p>Make this only accessibe to admin!</p>

<% @users.each do |user| %>
  <p>
      <%= link_to user.name, user_path(user.id) %>
  </p>
  <p>
      Email: <%= user.email %>
  </p>
  <p>
      Mobile: <%= user.mobile %>
  </p>

<% end %>
